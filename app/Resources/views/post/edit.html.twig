{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Editing post</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" class="btn btn-primary" value="保存" />
        {{ form_end(form) }}

    </div>
{% endblock body %}